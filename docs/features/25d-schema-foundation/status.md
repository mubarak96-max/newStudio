# 2.5D Book Experience — Status

Plan: [plan.md](plan.md) · Prompts: [prompts.md](prompts.md)

## Phase table

| Phase | State | Depends on | Date | Gates | Notes |
|---|---|---|---|---|---|
| P0 | done | — | 2026-09-18 | lint pass; build pass; smoke:workflow 9/9; smoke:ui 6/6; smoke:operations 13/13 | Behavior-neutral split only: image-studio 916→225+4 focused modules, worker/index 635→202+4 job helpers; routes/gates/costs/recovery unchanged; patch .patches/P0-studio.patch + new-file copies. |
| P1 | done | P0 | 2026-09-18 | lint pass; build pass (20 routes); smoke:auth 32/32; smoke:workflow 9/9; smoke:ui 6/6 | Code complete unattended: unified server auth mode, dev-only auto browser identity, bypass-aware readiness, sign-out hidden when bypassed, production forced even with flag. Owner must verify fresh-browser dev access and production rejection (steps in Progress log); live identity issuance never exercised. Patch .patches/P1-studio.patch + new-file copies. |
| P2 | done | P1 | 2026-09-18 | lint pass; build pass (22 routes); smoke:schemas 14/14; test:preview 23/23; Playwright prior art 8/8 (attempt-1 run, same tree) | Deterministic room-entry slice: Zod schemas, compileFixtureBundle + pure resolveFrame, local /api/studio/preview, /studio/preview with WebGL depthMesh + forced CSS fallback, HTML subtitles, scope/play/pause/scrub/next/previous only. Patch .patches/P2-studio.patch + new-file copies. |
| P3 | done | P2 | 2026-09-18 | lint pass; build pass (24 routes); smoke:schemas 14/14; smoke:canonical 17/17; smoke:pdf 2/2; smoke:uncertainties 5/5; smoke:workflow 10/10 | Canonical source + full section-3 schemas: p000000 seq, immutable canonical.json pointer, 20-para chunks, correction history with draft invalidation, classification instead of deletion, fakes-backed review/approval slice. Patch .patches/P3-studio.patch + new-file copies. |
| P4 | qa-pending | P3 | 2026-09-18 | lint pass; build pass (24 app routes + proxy, incl. …/jobs/approve); smoke:job-engine 23/23; smoke:operations 13/13; smoke:batch 4/4; smoke:workflow 10/10 | Code complete unattended (commits 972b1ff, 97bc450; zero source edits this attempt): one-document-per-job engine, provider boundaries, approval pipeline, lease recovery, idempotent costs; owner must verify lease recovery, approval idempotency and cost evidence in fixture runs plus confirm no visual auto-start (steps in Progress log); live kill/restart belongs to P13. Patch .patches/P4-studio.patch + new-file copies. |
| P5 | qa-pending | P4 | 2026-09-18 | lint pass; build pass (25 app routes + proxy, incl. …/model); smoke:book-model 29/29; smoke:schemas 14/14; smoke:bible 8/8 | Code complete unattended: rolling ledger + Book Model review; owner must inspect deterministic replay, evidence validity and retained approvals in fixture runs plus approve/reject/regenerate without entering data (steps in Progress log); real-book model quality belongs to P13. Patch .patches/P5-studio.patch + new-file copies. |
| P6 | done | P5 | 2026-09-18 | lint pass; build pass (26 routes); smoke:story-planning 31/31; smoke:episodes 18/18; smoke:moments 21/21 | AI StoryMap/Episode/Moment ranges with strict tiling, bounded repair, approval-only UI; later-episode production independent of published siblings. Patch .patches/P6-studio.patch + new-file copies. |
| P7 | done | P6 | 2026-09-18 | lint pass; build pass (26 routes); smoke:beats 42/42; smoke:text 10/10; smoke:posting pass; test:preview 23/23 | AI Beat planning with complete-book representation: structured quotes/dialogue/commentary, sentence grounding, verbatim offsets, reveal-safe views, semantic cameras/transitions, BeatReview with subtitles + coverage + pending-visual placeholders, no camera/text entry. Patch .patches/P7-studio.patch + new-file copies. |
| P8 | done | P7 | 2026-09-18 | lint pass; build pass (27 routes); smoke:visual-generation 50/50; smoke:images 6/6; smoke:batch 4/4; smoke:visual 10/10 | Reference-first single/batch generation via ImageProvider + fake S3 boundary: approved visualProfile/reference versions gate scenes, one coordinate frame per composition, clean backgrounds with hidden coverage, RGBA foregrounds, honest depth bit depth, targeted regeneration reusing approved siblings by immutable key. Patch .patches/P8-studio.patch + new-file copies. |
| P9 | qa-pending | P8 | 2026-09-18 | lint pass; build pass (28 routes); smoke:compose 51/51; smoke:beats 42/42; test:preview 51/51 | Code complete unattended: assembly, safeCamera, Episode preview with blockers and approve-and-continue; owner must play all scopes, test extremes/crops/motion/fallback, follow a rejected layer to regeneration, and visually certify art quality (steps in Progress log); paid regeneration never exercised. Patch .patches/P9-studio.patch + new-file copies. |
| P10 | in-progress | P9 | 2026-09-19 | Not run | P9 is qa-pending and permits P10; implementation and local fixture verification are in progress. |
| P11 | todo | P10 | — | Not run | Fresh-session retry requested by owner. |
| P12 | qa-pending | P11 | 2026-09-19 | 26/28 listed commands pass; lint/build + workflow/ui/auth/schemas/canonical/job-engine/book-model/story-planning/beats/visual-generation/compose/episodes/bible/moments/text/visual/images/batch/posting/completion/operations/pdf/uncertainties/preview pass; smoke:bundles and smoke:publishing fail (missing P10 scripts) | P3–P9 canonical fixture 10/10 through approvals, single+batch visuals, composition and all preview scopes; six uncalled temporary adapters contracted. Gaps: P10 publish/replace + bundle scripts absent; P11 rules absent; active primary flow still requires legacy allJobs/BibleEntry/EpisodePlan/StoryMomentPlan paths, so no claim that published v1 survives regeneration or that every retired path is gone. Patch `.patches/P12-studio.patch` + `.patches/P12-new-files/`. |
| P13 | blocked | P12 | — | Not run | HUMAN-ONLY; D-1 requires owner-run real model sample and release checks. |

## Progress log

### 2026-09-19 — P12 qa-pending: contract cleanup and degraded complete-workflow verification (unattended)

- Baseline before P12 edits: application source/tests were clean. Existing tracked changes were runner-owned only: deleted `.run/P4.result`–`P9.result` and `.run/settled.P4`–`settled.P9`, modified `.run/inflight`, `.runinfo`, `loop.sh`, and `status.md`. Existing untracked entries were `.run/swept.P12`, `logs/P11-attempt1-111213.log`, and `logs/P12-attempt1-111250.log`. `new.md` was not listed and remains untouched. P12 made no staging/commit/deploy/live-write changes.
- Dependency check: P11 was `blocked: Not started`; inspection also confirmed P10 was `blocked: Not started`. The missing P10 surface includes `src/features/book-experience/publishing/`, `worker/publishing/`, `publishEpisode`, `promoteRevision`, and package scripts `smoke:bundles` / `smoke:publishing`; P11 canonical access-rule behavior is also absent. The workflow fixture therefore uses the smallest honest publication placeholder and labels the missing boundary instead of recreating P10/P11.
- Contracted six temporary, production-uncalled adapters after `rg` proved their only callers were smoke tests: `src/features/bible/adapters.ts`, `episodes/adapters.ts`, `moments/adapters.ts`, `jobs/adapters.ts`, `images/adapters.ts`, and `visual/adapters.ts`. Moved Book Model derived-context, story-planning, job-engine, and visual-generation tests directly onto canonical contracts; lint/build and all affected smoke suites pass. Active legacy stores/types/components were preserved because they still have callers and removing them would break supported extraction, model settings, costs, and the current primary flow.
- Added `tests/book-experience/complete-workflow.mts` and chained it to `smoke:workflow`. The local fake fixture approves an immutable source with classified non-story material; validates and approves AI StoryMap/Episode/Moment plans; builds and approves three Beats; proves 3/3 story paragraphs have meaningful Representation links; runs one selected visual individually and two as a selected batch through FakeImageProvider/FakeS3Store; approves three immutable assets; assembles three safe compositions; compiles one canonical EpisodeBundle; resolves readable frames for Episode, Moment, and Beat scopes; and regenerates one draft target under a distinct immutable key while the version-one preview snapshot remains byte-identical. It explicitly reports `GAP Publication/replacement is not simulated: P10 + P11 are missing`.
- Actual evidence: `smoke:workflow` reports `10/10 checks passed; 1 explicit degraded-dependency gap`, including `PASS Every story paragraph has a meaningful canonical Beat representation`, `PASS Owner-selected single and batch visual generation share fake provider/repository boundaries`, `PASS The exact canonical bundle supports Episode, Moment and Beat preview scopes`, and `PASS Temporary schema adapters are contracted after canonical callers and tests moved`.
- Gates, baseline and post-change: 26/28 listed commands pass both before and after P12. Passing: lint; build (28 routes); smoke:workflow; smoke:ui; smoke:auth 32/32; smoke:schemas 14/14; smoke:canonical 17/17; smoke:job-engine 22/22 after adapter contraction; smoke:book-model 29/29; smoke:story-planning 29/29 after adapter contraction; smoke:beats 42/42; smoke:visual-generation 46/46 after adapter contraction; smoke:compose 51/51; smoke:episodes 18/18; smoke:bible 8/8; smoke:moments 21/21; smoke:text 10/10; smoke:visual 10/10; smoke:images 6/6; smoke:batch 4/4; smoke:posting 11/11 + rules static 5/5; smoke:completion 7/7; smoke:operations 13/13; smoke:pdf 2/2; smoke:uncertainties 5/5; test:preview 51/51. Failing unchanged baseline defects: `npm.cmd run smoke:bundles` and `npm.cmd run smoke:publishing` each exit 1 with `Missing script` because P10 did not land.
- QA remains pending: no published EpisodeBundle/catalog/pointer exists to prove version 1 survives draft regeneration; no P11 emulator behavior exists; Playwright browser interaction was not run beyond the required `test:preview` gate; and current `smoke:workflow` / `smoke:operations` still prove the active `allJobs` path while the Studio primary source page still imports legacy Bible/Episode/Moment consumers. Owner acceptance must wait for P10/P11 plus a browser pass producing the fixture using approvals and generation selections only.
- Diff audit: P12 tracked changes are package.json; four smoke files; six adapter deletions; status.md. New non-secret file: `tests/book-experience/complete-workflow.mts` (331 lines). All changed/new files are below 1000 lines; no new dependency, secret, Reader, payment/distribution feature, cloud deletion, remote write, plan/prompts/CONTEXT edit, or owner-file change. `git diff --check` reports only pre-existing runner whitespace in `.run/inflight:1`; left untouched as unrelated runner state.
- Patch: `.patches/P12-studio.patch` (tracked binary diff) plus explicit copy `.patches/P12-new-files/tests/book-experience/complete-workflow.mts`. No staging and no commit. Suggested commit message: `P12: contract dead schema adapters and verify the canonical local workflow`.

### 2026-09-18 — P9 qa-pending: automatic 2.5D assembly and actionable previews (unattended)

- Baseline (pre-edit): P8 done, P9 row in-progress. Tracked diff was runner state plus landed-but-uncommitted P5–P8 work (`package.json` scripts, `worker/commentary.mts`, `worker/image-api.mts`, `.patches/P4-studio.patch` regeneration, `.run/inflight`, loop log, `status.md`); untracked entries were P5–P8 patch artifacts, runner logs/results and landed-but-uncommitted P5–P8 code (`src/app/api/books/[bookId]/episodes|model|visuals/`, `book-experience/beats|book-model|coverage|story-map|visuals/`, adapters, `worker/beats|book-model|story-planning|visuals/`, three smoke files). `new.md` untouched (still untouched after this phase). P9 work is unstaged only (tracked edits: `package.json` one-line script, `src/app/api/studio/preview/route.ts` one-line fixture registration, `src/features/book-experience/preview/index.ts` export appendix, `tests/book-experience/preview-check.mts` extension; new files listed below).
- Dependency check: P8 done permits progress. No placeholder; all P9 work is real code against fakes/fixtures. No paid regeneration, no live writes, no weakened camera/coverage validation, no camera/layer/text entry by the owner (ADR-003).
- Read AGENTS.md plus bundled Next guide `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md` before changing Next code (new compositions route uses the documented GET/POST handler pattern with `await context.params`, matching the episodes/model/visuals routes).
- New domain `src/features/book-experience/compositions/`: `assemble.ts` (assembleComposition/compositionApprovalProblem: shared-canvas validation, foreground alpha, depthMesh depth+mesh alignment, background overscan, structural failures yield no composition, content failures stay inspectable but blocked), `safeCamera.ts` (computeSafeCamera from actual overscan coverage and depth displacement, clampCameraPose/isCameraSafe/replanCameraToSafe, UNSAFE_CAMERA blocks), `resolve.ts` (resolveSemanticTarget/unresolvedShotEntities to layer IDs, buildResponsive focal crops, validateTransition, deriveMomentCompositions from usedIn per ADR-011), `actions.ts` (getNextAction: approve-and-continue/generate selected/regenerate layer/retry AI planning/preview with named blockers and targetActionId), `repository.ts` (FakeCompositionStore: immutable proposals, fingerprint approvals, outdated invalidation, canonical composition path), `components/CompositionReview.tsx` (read-only layer/depth inspector with parallax scrub, AI cameras with rationale, exact resolving action per blocker; verified no textarea/text/number/camera inputs by source grep).
- Preview extensions `src/features/book-experience/preview/`: `transitions.ts` (executeTransition for cut/fade/slide/zoomThrough/parallaxShift with deterministic reduced-motion static), `responsive.ts` (selectResponsiveCrop portrait/landscape, focalVisible, isCropContained), `states.ts` (explicit empty/loading/error/outdated/partial/ready with honest placeholders), `EpisodePreview.tsx` (direct Beat/Moment/Episode scopes over the exact bundle contract, transition execution in both stages, webglcontextlost → deterministic CSS fallback, layer/depth inspector, blockers with resolve buttons, approve/generate/regenerate actions; no camera/layer/text inputs).
- Fixture `fixtures/book-experience/harbor-composed.json` (deterministic local fake: ep_01, ep_01_m01, 2 Beats with verbatim whole-paragraph quote/dialogue, cmp_harbor_v1 with L0 plane + L1 depthMesh, safeCamera 0.1/0.08/1.1/0.08, responsive crops) registered in the preview endpoint alongside room-entry.
- Worker `worker/compositions/assemble.mts` (runCompositionAssembly through FakeCompositionStore, AI replan flags per Beat, approveComposedComposition requires zero issues). Thin route `src/app/api/books/[bookId]/compositions/route.ts` (GET status counts; POST compositions.approve with fingerprint compare-and-swap and zero-issue gating / compositions.regenerate failed-only with reason and distinct idempotency key). Thin page `src/app/studio/books/[bookId]/compose/` (ComposeWorkspace over the fixture bundle with explicit loading/error states). Every new file <1000 lines; plan.md, prompts.md, CONTEXT.md untouched; no new dependencies; no secrets in new files.
- Test seams: `tests/smoke/smoke-compose.mts` 51/51 (prior art: smoke-image-prompt.mts PASS style, preview.spec.ts from P2). Covers assembly valid/structural/content failures, safeCamera extremes with clamp/replan, semantic resolution, responsive crops, transition validation, next-action blockers with targetActionId, fingerprint approvals with outdated invalidation, worker pass plus blocked-approve refusal, rejected-layer → regeneration → clean re-assembly → real rendering progression (harbor fixture compiles, every Beat resolves finite frames/transitions/subtitles), UI input-absence, route gating, file sizes. Extended `test:preview` 23→51 checks (harbor source-to-episode progression, all five transitions, crops, all six preview states, EpisodePreview surface with context-loss handling, assembly seam probes, smoke:compose declaration; all 23 prior checks unchanged and green). One test-authoring fix: the alpha-block assertion matched the issue code instead of the human message; corrected to the message text (behavior unchanged). One build fix: ReferencePin imports from schemas/model.ts, not primitives.ts (behavior unchanged).
- Gates (all green): lint pass; build pass (route table 27→28 with …/compositions plus …/compose page); smoke:compose 51/51; smoke:beats 42/42; test:preview 51/51. Evidence: "PASS Blocked approval names the layer-regeneration action" and "PASS Clean review enables approve-and-continue with no camera/layer/text entry" and live `next start` endpoint returning `"provenance":"fixture harbor-composed: deterministic local fake…"`, ep_01 with 2 Beats and exact quote "Mara pushed the door open…" — an assembled Episode previews end to end and each blocker names its resolving action. The temporary server was stopped afterwards (no listener left on :8900/:8901).
- Demoable outcome met locally: EpisodePreview + CompositionReview expose the assembled Episode with layer/depth inspection, per-blocker resolve buttons and approve-and-continue, and the smoke run rejects a foreground layer, regenerates it alone and returns to an approvable preview. OWNER STEPS REMAIN (qa-pending, do not mark done until): (1) Before: deterministic harbor-composed fixture was used; substitute owner-supplied generated assets when available, never paid regeneration in this phase. (2) During: confirm no paid regeneration ran and no camera/coverage validation was weakened. (3) After: play Beat, Moment and Episode scopes; test camera extremes, portrait/landscape, reduced motion and forced CSS fallback; follow a rejected-layer blocker to regeneration and back to preview with no manual scene authoring; visually inspect silhouettes, holes, layer order, subtitle legibility, motion comfort and fallback — browser automation cannot certify artistic quality. Note: `/studio/books/[bookId]/compose` returned 307 without a session (auth proxy, expected); verify signed-in/dev-bypass access in the browser pass. Real-asset assembly quality remains P13/D-1 owner QA.
- Patch: `.patches/P9-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P9-new-files/`. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P9: automatic 2.5D assembly with safeCamera and actionable episode preview`.
- Out of scope (logged, not fixed): legacy visual/images/composition consumers still serve the old one-image model until P10–P12 migrate them; non-gate suites not re-run; smoke-e2e/smoke-formats remain forbidden as autonomous gates per Scaffold deferred.

### 2026-09-18 — P8 done: immutable visual references and single/batch generation (unattended)

- Baseline (pre-edit): P7 done, P8 row in-progress. Tracked diff was runner state only (`.patches/P4-studio.patch` regeneration, `.run/inflight`, `loop-run-20260918-221559.log`, `status.md`, `package.json` one-line P7 script, `worker/commentary.mts` P7 appendix); untracked entries were P5/P6/P7 patch artifacts, runner logs/results and landed-but-uncommitted P5/P6/P7 code plus prior-attempt `logs/P8-attempt1-002422.log` (no P8 source had landed). `new.md` untouched (still untouched after this phase). P8 work is unstaged only (tracked edits: `package.json` one-line script, `worker/image-api.mts` pure key-builder appendix; new files listed below).
- Dependency check: P7 done permits progress. No placeholder; all P8 work is real code against fakes/fixtures. No paid generation, no live writes, no segmentation/depth/inpaint vendors, no manual upload/edit tools (ADR-008/ADR-012).
- Read AGENTS.md plus bundled Next guide `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md` before changing Next code (new visuals route uses the documented GET/POST handler pattern with `await context.params`, matching the episodes/model routes).
- New domain `src/features/book-experience/visuals/`: `references.ts` (AI-proposed visualProfile/reference sets, fingerprint-idempotent approvals, referenceApprovalProblem blocks scenes until every pinned reference is approved, pinReferences), `requests.ts` (assembleVisualRequest/assembleVisualBatch: prompt + negativePrompt per target kind, one shared coordinate frame 1600x2000/overscan-160 and one pinned reference-version set per composition, distinct idempotency key per attempt, 25-target batch bound), `assets.ts` (validateAsset: frame match, foreground real alpha, background hidden coverage, depth alignment + honest sourceBitDepth + near-white convention, text-free, checksummed), `regeneration.ts` (planRegeneration at asset/layer/composition scope with distinct key, regenerationPreservesSiblings), `repository.ts` (FakeVisualStore: immutable visualVersions/composition paths, idempotent per-unit charges, approve/reject generations, approvedSiblingKeys, descriptorFor), `components/VisualReview.tsx` (approved references beside candidates, multi-select generation, targeted regeneration, blockers; Approve/Generate-selected/Regenerate only — verified no textarea/text/camera/promptAddition inputs by source grep).
- Adapters preserving internals: `src/features/visual/adapters.ts` (StyleCanon→visualProfile draft, legacy reference gating), `src/features/images/adapters.ts` (ready candidates→AssetRef without inventing depth/alpha). Thin route `src/app/api/books/[bookId]/visuals/route.ts` (GET profile/request status; POST references.approve / generate single-or-batch with approval gating / regenerate failed-only with reason; no prompt editing, ADR-003).
- Worker `worker/visuals/`: `generate.mts` (runVisualGeneration through the configured ImageProvider shape + FakeS3Store immutable boundary, records S3 key/source/reference versions/bit depth/prompt/model/cost/continuity; approveVisualGeneration requires zero issues), `batch.mts` (runVisualBatch through the existing bounded runWithConcurrency pool). `worker/image-api.mts` gains pure `visualAssetKeyFor` (`books/{bookId}/assets/{assetVersionId}/{role}.{ext}`, plan 4); live upload transport untouched. `worker/image-batch.mts` intentionally untouched — reused via the pool, proven by the still-green smoke:batch gate. Every new file <1000 lines (largest: smoke 412); plan.md, prompts.md, CONTEXT.md untouched; no new dependencies; no secrets in new files.
- Test seam `tests/smoke/smoke-visual-generation.mts` 50/50 (prior art: smoke-image-prompt/batch/visual-system PASS style). Fixture: approved ch_mara + loc_harbor references, one background + one foreground + one depth target in composition cmp_harbor_v1. Covers pinning/shared frame, batch/shared path, approval-gating of premature scenes, immutable S3 keys/charges, batch failure isolation + failed-only retry, all eight validation negatives plus honest-16-bit-container acceptance, clean-approve/dirty-block, foreground reject → layer-scope regeneration with approved background byte-identical, reference-change flagging with pinned provenance retained, key shape, legacy adapters, UI input-absence, route gating, file sizes. One gate repair: removed a non-existent `lens` field from the StyleCanon fixture (build type-check) and expanded a TS parameter property (strip-types) — behavior unchanged.
- Gates (all green): lint pass; build pass (route table 26→27 with …/visuals); smoke:visual-generation 50/50; smoke:images 6/6; smoke:batch 4/4; smoke:visual 10/10. Evidence: "PASS Only the affected target regenerates" and "PASS Approved assets remain unchanged during sibling regeneration" and "PASS Reference approval blocks premature scene generation" — an approved background keeps its exact S3 key/checksum while a rejected foreground regenerates alone, and unapproved references block scene requests.
- Demoable outcome met locally: VisualReview exposes approved references beside generated candidates with Generate-selected and per-target Regenerate actions; the smoke run approves consistent references, generates one target plus a batch, rejects a foreground asset and regenerates only it. Owner QA (one-line spot check, local only): render VisualReview with approved ch_mara/loc_harbor references, generate bg-harbor alone then fg-mara + depth-harbor as a batch, reject the foreground, regenerate it and confirm the background key/checksum never changed — all covered by the green smoke; real-model asset quality remains P13/D-1 owner QA.
- Patch: `.patches/P8-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P8-new-files/`. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P8: reference-first visual generation with single/batch selection and targeted regeneration`.
- Out of scope (logged, not fixed): legacy visual/images stores, routes and UIs still serve the old styleCanon/one-image model until P9–P12 migrate them; non-gate suites not re-run; smoke-e2e/smoke-formats remain forbidden as autonomous gates per Scaffold deferred.

### 2026-09-18 — P7 done: AI Beat planning and complete-book representation (unattended)

- Baseline (pre-edit): P6 done, P7 row in-progress. Tracked diff was runner state only (`.patches/P4-studio.patch` regeneration, `.run/inflight`, `loop-run-20260918-221559.log`, `status.md`, `package.json` one-line P6 script); untracked entries were P5/P6 patch artifacts, runner logs/results and landed-but-uncommitted P5/P6 code (`src/app/api/books/[bookId]/episodes|model/`, `book-experience/book-model|story-map/`, `worker/book-model|story-planning/`, smoke-book-model/story-planning, adapters). `new.md` untouched (still untouched after this phase). P7 work is unstaged only (tracked edits: `package.json` one-line script, `worker/commentary.mts` pure verifier appendix; new files listed below).
- Dependency check: P6 done permits progress. No placeholder; all P7 work is real code against fakes/fixtures. No paid planning calls, no live writes, no camera/text entry by the owner (ADR-003).
- Read AGENTS.md plus bundled Next guide `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md` before changing Next code (new BeatReview is a `"use client"` review component with approve/regenerate buttons only, matching StoryMapReview).
- New domain `src/features/book-experience/beats/`: `build.ts` (buildBeats/beatIdFor/splitLongText: source-order Beats ep_01_m01_b01, ~60-word splits with exact UTF-16 offsets and zero content loss, seq-gated entities, one compositionId per Beat, deterministic AI camera/transition plans with rationale), `verbatim.ts` (validateVerbatim: slice-exact quote/dialogue match, no normalization), `representations.ts` (validateRepresentations: meaningful descriptions ≥12 chars, generic-tag rejection, internal paragraphs require subtitle text, source-order enforcement), `reveal.ts` (resolveRevealView/isRevealSafe: pre-reveal names hidden below reveal seq), `repository.ts` (FakeBeatStore: propose/approve with fingerprint idempotency, correction-driven outdated invalidation), `components/BeatReview.tsx` (subtitles + paragraph-to-Beat coverage table + pending-visual placeholders; Approve/Request-AI-revision only, no camera/text inputs — verified by source grep).
- Coverage `src/features/book-experience/coverage/review.ts` (buildCoverageReview/coverageApprovalProblem: uncovered/meaningless paragraphs block approval with named IDs; non-story stays classified outside coverage). Moment-text `src/features/moment-text/grounding.ts` (splitSentences/verifyCommentary: sentence-level citations, Moment-mapping checks, blocked future-term leak detection).
- Worker `worker/beats/plan.mts` (runBeatPlanning with injected fake proposer, Moment-approval gating, verbatim+representation validation before store); `worker/commentary.mts` gains a pure verifyCommentary appendix (existing generateCommentary untouched). Package script `smoke:beats` added. Every new file <1000 lines; plan.md, prompts.md, CONTEXT.md untouched; no new dependencies; no secrets in new files.
- Test seam `tests/smoke/smoke-beats.mts` 42/42 (prior art: smoke-moment-text.mts, smoke-posting.mts PASS style; preview.spec.ts from P2 reused via compileFixtureBundle+resolveFrame). Fixture: dialogue + 120-word long passage + internal thought + classified non-story + reveal paragraph. Covers order/IDs, lossless split, internal meaning in escaped HTML subtitles, verbatim pass/mutation-fail, tagging/gap/internal/order negatives, reveal hide/show, seq gating, camera completeness, Zod parsing, sentence grounding + leak blocks, worker pass/approval-idempotency/outdated-invalidation, coverage approve/block, fixture-preview reuse, UI input-absence, file sizes. One test-authoring fix: the seq-gating check first expected an allowed entity to vanish; corrected to gate out every entity (behavior unchanged).
- Gates (all green): lint pass; build pass (26 routes, no new routes — P7 adds no endpoints by design); smoke:beats 42/42; smoke:text 10/10; smoke:posting pass; test:preview 23/23. Evidence: "PASS Split quotes drop no content" and "PASS Internal thought survives in subtitle commentary" and "PASS Future identity stays hidden before its seq" and "PASS Malformed coverage blocks approval" — a long passage splits losslessly, an internal thought persists in subtitles with exact offsets, future identities hide until their seq, and gap coverage blocks approval.
- Demoable outcome met locally: BeatReview exposes AI-planned Beats with readable subtitles, a complete paragraph-to-Beat coverage review and pending-visual placeholders, with Approve/Request-AI-revision as the only actions. Owner QA (one-line spot check, local only): render BeatReview with the smoke fixture Beats, confirm the long passage reads whole across two subtitles, the internal thought is fully worded, quotes match the source offsets, the deckhand identity hides until p000004, and removing a paragraph representation blocks Approve.
- Patch: `.patches/P7-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P7-new-files/`. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P7: AI beat planning with verbatim, reveal-safe coverage review`.
- Out of scope (logged, not fixed): legacy moment-text store/routes/UIs still serve the old primary-text/one-image model until P8–P12 migrate them; non-gate suites not re-run; smoke-e2e/smoke-formats remain forbidden as autonomous gates per Scaffold deferred.

### 2026-09-18 — P6 done: StoryMap, Episodes and Moments (unattended)

- Baseline (pre-edit): P5 qa-pending, P6 row in-progress. Tracked diff was runner state only (`.patches/P4-studio.patch` regeneration, `.run/inflight`, `loop-run-20260918-221559.log`, `status.md`, `package.json` one-line P5 script); untracked entries were P5 patch artifacts, runner logs/results (incl. `logs/P6-attempt1-235710.log`) and landed-but-uncommitted P5 code (`src/app/api/books/[bookId]/model/`, `src/features/book-experience/book-model/`, `worker/book-model/`, `smoke-book-model.mts`, `bible/adapters.ts`). `new.md` untouched (still untouched after this phase). P6 work is unstaged only (tracked edit: `package.json` one-line script; new files listed below).
- Dependency check: P5 qa-pending permits progress. No placeholder; all P6 work is real code against fakes/fixtures. No paid planning calls, no live writes, no boundary editing by the owner (owner checklist Before/During).
- Read AGENTS.md plus bundled Next guide `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md` before changing Next code (new episodes route uses the documented `await context.params` pattern, matching the canonical/model routes).
- New domain `src/features/book-experience/story-map/`: `coverage.ts` (validateStoryCoverage/validateEpisodeCoverage/validateMomentCoverage: zero-based seq, inclusive ends, p000000 ids, exact-once tiling, no gaps/overlaps/exclusions in story scope), `planning.ts` (planStoryMap/planEpisode through canonical Zod contracts; planMoments builds contiguous ep_01_m01 scene containers in book order; IDs ep_01/ep_01_m01 per CONTEXT.md), `repair.ts` (repairStoryCoverage with capped attempts and explicit STORY_REPAIR_BUDGET_EXHAUSTED, never a blank form), `progression.ts` (getStoryPlanningActions approve/regenerate/preview with named blockers; canProduceEpisode proves a published ep_01 never gates ep_02), `repository.ts` (FakeStoryPlanningStore: storyMap/episodes/moments proposals, idempotent fingerprint approvals, independent publishEpisode), `components/StoryMapReview.tsx` (acts/arcs/chronology, paragraph timeline with Episode boundaries, source highlights, stage chips, Approve/Request-AI-revision only) and `components/EpisodeBoard.tsx` (Moment scene ranges, per-stage chips, coverage, Approve-Moments/Preview readiness; no boundary/field inputs anywhere — verified by source grep).
- Adapters preserving internals: `src/features/episodes/adapters.ts` (re-exports coverageProblems/moveBoundary; legacy p000001 ranges map to canonical seq; intentional-overlap/exclusion support retired, not carried), `src/features/moments/adapters.ts` (re-exports momentCoverageProblems/momentRejection; contiguous legacy mappings convert, many-to-many returns null for AI replanning; primary-text/one-image retired to P7 Beat scope).
- Worker `worker/story-planning/`: `story-map.mts` (runStoryMapPass with injected fake proposer + bounded repair), `episodes.mts` (runEpisodePlanning gated on StoryMap approval), `moments.mts` (runMomentPlanning with contiguity enforcement), `repair.mts` (budget-explicit wrapper).
- Thin route `src/app/api/books/[bookId]/episodes/route.ts` (GET review status; POST story-map.approve/regenerate and moments.approve/regenerate with StoryMap-approval gating; approve/reject/regenerate only, no boundary editing, ADR-003). Package script `smoke:story-planning` added. Every new file <1000 lines (largest: smoke 393); plan.md, prompts.md, CONTEXT.md untouched; no new dependencies; no secrets in new files.
- Test seam `tests/smoke/smoke-story-planning.mts` 31/31 (prior art: smoke-episode-map.mts, smoke-moments.mts PASS style). Fixture story seq 0–9: strict tiling, gap/overlap/order/range rejections with named ranges, Moment contiguity, Zod parsing of fake AI StoryMap/Episode, bounded repair success + budget exhaustion, idempotent approvals, publish-ep_01-keeps-ep_02-producible, progression blockers naming the AI-repair action, legacy adapter mapping, worker passes with fake proposers, p000000 identity. One early failure fixed honestly: domain files used the `@/` alias which Node strip-types cannot resolve outside Next — switched to relative imports (behavior unchanged).
- Gates (all green): lint pass; build pass (route table 25→26 with …/episodes); smoke:story-planning 31/31; smoke:episodes 18/18; smoke:moments 21/21. Evidence: "PASS Valid proposal approves without changing boundaries manually" and "PASS Invalid gaps trigger bounded AI repair" and "PASS Publishing ep_01 never blocks ep_02 production" — a fixture map with a gap and overlap is repaired/blocked by bounded AI replanning, the valid proposal approves through named next-action buttons with no boundary edits, and a published earlier Episode never gates its sibling.
- Demoable outcome met locally: StoryMapReview + EpisodeBoard expose AI-proposed Episode/Moment ranges with complete story-coverage state and clear Approve/Preview/Request-AI-revision buttons; progression logic proves the approval path end to end. Owner QA (one-line spot check, local only): open the StoryMap review with the fixture map, inject a gap and an overlap, confirm repair/blocking banners name the AI-revision action, then approve the valid proposal without touching boundaries and confirm the Moment timeline tiles the Episode exactly.
- Patch: `.patches/P6-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P6-new-files/`. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P6: AI story planning with strict coverage, bounded repair and approval review`.
- Out of scope (logged, not fixed): legacy episode/moment stores, routes and UIs still serve the old intentional-overlap/many-to-many model until P7–P12 migrate them; non-gate suites not re-run; smoke-e2e/smoke-formats remain forbidden as autonomous gates per Scaffold deferred.

### 2026-09-18 — P5 qa-pending: rolling ledger and redesigned Book Model (unattended)

- Baseline (pre-edit): P4 qa-pending (commits 972b1ff, 97bc450), P5 row in-progress. Tracked diff was runner state only (`.run/inflight`, `loop-run-20260918-221559.log`, `status.md`, `.patches/P4-studio.patch`); untracked entries were runner logs only (`logs/P4-attempt1-*.log`, `logs/P5-attempt1-233705.log`). `new.md` untouched (still untouched after this phase). P5 work is unstaged only (tracked edit: `package.json` one-line script; new files listed below).
- Dependency check: P4 qa-pending permits progress. No placeholder; all P5 work is real code against fakes/fixtures. No paid understanding calls, no live queue, no change to the no-fabricated-facts rule (owner checklist Before/During).
- Read AGENTS.md plus bundled Next guide `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md` before changing Next code (new model route uses the documented `await context.params` pattern, matching the canonical/approve routes).
- New domain `src/features/book-experience/book-model/`: `windows.ts` (chapter-bounded windows, overlap dedup, compact entity/alias context), `ledger.ts` (LedgerDeltaSchema, applyLedgerDelta/replayLedger/ledgerHash, immutable checkpoint keys), `consolidate.ts` (evidence-gated consolidation, quote grounding, fills never facts, approved decisions survive regeneration), `state.ts` (resolveEntityState by storyTime, reveal visibility by seq, spoiler-safe displayIdentityAtSeq, state reference keys), `proposals.ts` (findAliasConflicts/proposeAliasResolution, AI proposes merges/splits, approve/reject without entering data), `derived.ts` (terminology/chapter summaries as derived context), `repository.ts` (FakeBookModelStore: immutable per-window checkpoints, proposals, idempotent model approval), `components/BookModelReview.tsx` (searchable Entity table/detail, Event chronology with source links, facts vs fills, pre-reveal identity; approve/reject/regenerate actions only).
- Worker `worker/book-model/`: `understand.mts` (rolling pass over chapter-bounded windows with resume-from-checkpoint, fake LLM boundary), `consolidate.mts` (reject→AI-revision preserving locked decisions).
- Adapters: `src/features/bible/adapters.ts` (term/chapter Bible entries → derived context; generic entries not carried over per CONTEXT.md legacy vocabulary). Thin routes `src/app/api/books/[bookId]/model/route.ts` (GET review summary, POST approve/reject/regenerate; no entity field editing, ADR-003). Package script `smoke:book-model` added. Every new file <1000 lines (largest: smoke 333, review UI 236); plan.md, prompts.md, CONTEXT.md untouched; no new dependencies; no secrets in new files.
- Test seam `tests/smoke/smoke-book-model.mts` 29/29 (prior art: smoke-bible.mts PASS style, smoke-posting.mts). Inline alias/flashback fixture: "Mara"→"The Captain" alias change plus a seq-4/storyTime-1 flashback. Covers chapter-bounded windows, overlap dedup, deterministic replay, evidence/out-of-revision rejection, fabricated-quote rejection, fills-vs-facts, alias conflict proposal, approve/reject without data, storyTime state resolution, spoiler-safe identity (deckhand@2, Mara Quinn@4), checkpoint immutability/interruption, approval idempotency, derived context, schema parsing, reject→revision with retained spec, rolling-pass resume.
- Gates (all green): lint pass; build pass (route table 24→25 with …/model); smoke:book-model 29/29; smoke:schemas 14/14; smoke:bible 8/8. Evidence: "PASS Pre-reveal reader sees the deckhand identity" and "PASS Flashback resolves state by storyTime, not seq" and "PASS Rejected proposal regenerates while the approved spec survives" — a fixture with an alias change and flashback resolves correct state with spoiler-safe identity, a rejected proposal returns to AI revision with approvals kept, and approval needs no typed fields.
- Demoable outcome met locally: BookModelReview shows the AI-proposed model with paragraph evidence, separate storyTime (chronology sort) and reveal seq gating, and the model route approves with one action and no field entry. Owner QA below must confirm it on screen.
- OWNER STEPS REMAIN (qa-pending, do not mark done until): (1) run `npm.cmd run smoke:book-model` locally and inspect the alias/flashback PASS lines; (2) review the fixture with the alias change and flashback in the BookModelReview UI: verify correct state at storyTime 1 vs 10 and spoiler-safe identity at seq 2 (deckhand) vs seq 4 (Mara Quinn); (3) reject a proposal, inspect the AI revision (approved spec survives), then approve without entering data; (4) confirm no paid understanding call ran (fakes only) and the no-fabricated-facts rule is unchanged. Real-book model quality remains an owner check in P13.
- Patch: `.patches/P5-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P5-new-files/`. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P5: rolling ledger, consolidated book model and evidence review`.
- Out of scope (logged, not fixed): non-gate suites not re-run; legacy bible/episode/moment consumers still use generic entries until P6–P10 migrate them; P4 row route count now 24 app routes + proxy (P5 build shows 25 with …/model).

### 2026-09-18 — P4 qa-pending: verification pass, no code change (unattended, attempt 3)

- Baseline (pre-edit): P3 done, P4 code committed (972b1ff, 97bc450). Tracked diff was runner state only (`docs/features/25d-schema-foundation/loop-run-20260918-221559.log`, `status.md`, `.patches/P4-studio.patch`); untracked entries were runner logs only (`docs/features/25d-schema-foundation/logs/P4-attempt1-224753.log`, `P4-attempt1-232826.log`). `new.md` untouched (still untouched after this phase). This attempt made zero source edits: engine/pipeline/costs/providers, worker leases/orchestrate/providers, approve route, adapters and smoke-job-engine were re-read and re-gated, not rewritten.
- Dependency check: P3 done permits progress. No placeholder; all P4 work is real code against fakes/fixtures. No live queue, paid providers or budget-authority changes (owner checklist Before/During).
- Re-verified scope (plan sections 4/6/9, ADR-003/ADR-006/ADR-008/ADR-012): one job document per job (`books/{bookId}/jobs/{jobId}`) with transactional claims/leases, heartbeats, retries, checkpoints, cancellation, idempotency and bounded concurrency (`src/features/book-experience/jobs/engine.ts` 373 lines, `worker/jobs/leases.mts` 137 lines); LlmProvider/ImageProvider wrap existing OpenRouter transports with no depth/segment/inpaint vendors (`.../jobs/providers.ts` 134 lines, `worker/providers/`); approval of a completed planning checkpoint enqueues exactly the next eligible planning step, visuals only via explicit selection (`.../jobs/pipeline.ts` 204 lines, `worker/jobs/orchestrate.mts` 36 lines, `src/app/api/books/[bookId]/jobs/approve/route.ts` 164 lines); idempotent costs keyed `${idempotencyKey}:unit:${unit}` (`.../jobs/costs.ts` 70 lines); legacy allJobs callers untouched, adapters in `src/features/jobs/adapters.ts` remain until P12. Every scoped file <1000 lines; plan.md, prompts.md, CONTEXT.md, AGENTS.md, new.md untouched; package.json unchanged (no new dependencies); no secrets in P4 scoped files.
- Gates (all green, this attempt): lint pass; build pass (24 app routes + proxy, route table includes …/jobs and …/jobs/approve); smoke:job-engine 23/23; smoke:operations 13/13; smoke:batch 4/4; smoke:workflow 10/10. Evidence: "PASS Approve twice yields one successor job" and "PASS Resume runs only the remaining provider call" and "PASS Cost ledger holds one charge per completed unit" — a fixture checkpoint approval advances one planning job, a killed multi-step job resumes without repeating completed calls, and completed units are never double-charged.
- OWNER STEPS REMAIN (qa-pending, do not mark done until): (1) run `npm.cmd run smoke:job-engine` locally and inspect lease-recovery, approval-idempotency and cost-accounting PASS lines; (2) approve a fixture checkpoint twice and confirm exactly one successor planning job; (3) interrupt a fixture multi-step job mid-run, resume it, and confirm retained checkpoints with no duplicate completed-call charges; (4) confirm no `images.*` job exists without an explicit single/batch selection. Unattended run used fakes only; live kill/restart verification belongs to P13.
- Patch: `.patches/P4-studio.patch` (tracked binary diff, regenerated this attempt) plus new-file copies under `.patches/P4-new-files/` (verified current). No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P4: durable job engine, provider boundaries and approval-driven pipeline`.
- Out of scope (logged, not fixed): non-gate suites not re-run; worker `dispatch.mts` still serves the legacy allJobs map path by design until P12.

### 2026-09-18 — P4 qa-pending: verification pass, no code change (unattended)

- Baseline (pre-edit): P3 done, P4 code committed (972b1ff, 97bc450). Tracked diff was runner state only (`docs/features/25d-schema-foundation/loop-run-20260918-221559.log`, `status.md`); sole untracked entry was runner log `docs/features/25d-schema-foundation/logs/P4-attempt1-224753.log`. `new.md` untouched (still untouched after this phase). This attempt made zero source edits: engine/pipeline/costs/providers, worker leases/orchestrate/providers, approve route, adapters and smoke-job-engine were re-read and re-gated, not rewritten.
- Dependency check: P3 done permits progress. No placeholder; all P4 work is real code against fakes/fixtures. No live queue, paid providers or budget-authority changes (owner checklist Before/During).
- Re-verified scope (plan sections 4/6/9, ADR-003/ADR-006/ADR-008/ADR-012): one job document per job (`books/{bookId}/jobs/{jobId}`) with transactional claims/leases, heartbeats, retries, checkpoints, cancellation, idempotency and bounded concurrency (`src/features/book-experience/jobs/engine.ts` 373 lines, `worker/jobs/leases.mts` 137 lines); LlmProvider/ImageProvider wrap existing OpenRouter transports with no depth/segment/inpaint vendors (`.../jobs/providers.ts` 134 lines, `worker/providers/`); approval of a completed planning checkpoint enqueues exactly the next eligible planning step, visuals only via explicit selection (`.../jobs/pipeline.ts` 204 lines, `worker/jobs/orchestrate.mts` 36 lines, `src/app/api/books/[bookId]/jobs/approve/route.ts` 164 lines); idempotent costs keyed `${idempotencyKey}:unit:${unit}` (`.../jobs/costs.ts` 70 lines); legacy allJobs callers untouched, adapters in `src/features/jobs/adapters.ts` remain until P12. Every scoped file <1000 lines; plan.md, prompts.md, CONTEXT.md, AGENTS.md, new.md untouched; no new dependencies; no secrets added by this phase (only pre-existing `OPENROUTER_*` env reads and the plan's own vendor-prohibition text matched the secret scan).
- Gates (all green, this attempt): lint pass; build pass (24 app routes + proxy, route table includes …/jobs and …/jobs/approve); smoke:job-engine 23/23; smoke:operations 13/13; smoke:batch 4/4; smoke:workflow 10/10. Evidence: "PASS Approve twice yields one successor job" and "PASS Resume runs only the remaining provider call" and "PASS Cost ledger holds one charge per completed unit" — a fixture checkpoint approval advances one planning job, a killed multi-step job resumes without repeating completed calls, and completed units are never double-charged.
- OWNER STEPS REMAIN (qa-pending, do not mark done until): (1) run `npm.cmd run smoke:job-engine` locally and inspect lease-recovery, approval-idempotency and cost-accounting PASS lines; (2) approve a fixture checkpoint twice and confirm exactly one successor planning job; (3) interrupt a fixture multi-step job mid-run, resume it, and confirm retained checkpoints with no duplicate completed-call charges; (4) confirm no `images.*` job exists without an explicit single/batch selection. Unattended run used fakes only; live kill/restart verification belongs to P13. No new dependencies (zod only in scope; three/fiber/playwright untouched).
- Patch: `.patches/P4-studio.patch` (tracked binary diff, regenerated this attempt) plus new-file copies under `.patches/P4-new-files/` (verified current). No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P4: durable job engine, provider boundaries and approval-driven pipeline`.
- Out of scope (logged, not fixed): `smoke:ui`/`smoke:posting` and other non-gate suites not re-run; worker `dispatch.mts` still serves the legacy allJobs map path by design until P12; P4 row route count corrected 25→24 app routes (+ proxy) against the verified build table.

### 2026-09-18 — P4 qa-pending: durable jobs and automatic planning orchestration (unattended)

- Baseline (pre-edit): P3 done, P4 in-progress. Tracked diff was runner state only (.run/.runinfo/loop.sh); untracked only runner logs/pid. `new.md` untouched (still untouched after this phase). P4 implementation core had landed in an earlier commit (engine/pipeline/costs/providers, worker leases/orchestrate/providers, approve route, adapters, smoke-job-engine); this attempt repaired its gates and finished verification.
- Dependency check: P3 done permits progress. No placeholder needed; all P4 work landed as real code against fakes/fixtures. No live queue, paid providers or budget-authority changes (owner checklist Before/During).
- Fixes this attempt: (1) `src/features/book-experience/jobs/providers.ts` FakeImageProvider plus `worker/providers/llm.mts`/`images.mts` OpenRouter providers used TS constructor parameter properties, which Node `--experimental-strip-types` cannot parse — expanded to explicit field declarations with identical behavior. (2) `tests/smoke/smoke-job-engine.mts` heartbeat/cancel/retry block ran against the shared store where earlier queued successors shadowed the new jobs — isolated to a fresh FakeJobStore/FakeClock, and the repair check now asserts the default budget exhausts (REPAIR_BUDGET_EXHAUSTED) before an explicit `maxRepairs: 2` repair requeues. (3) Added missing `smoke:job-engine` npm script (gate requirement). (4) `smoke:workflow` script gained `--experimental-strip-types` (same convention as commit 0021a2a): its dynamic `.ts` imports fail on Node v22.16 without the flag; file content unchanged, still 10/10.
- Scope landed (plan sections 4/6/9, ADR-003/ADR-006/ADR-008/ADR-012): one job document per job (`books/{bookId}/jobs/{jobId}`) with transactional claims/leases, heartbeats, retries, checkpoints, cancellation, idempotency and bounded concurrency (`src/features/book-experience/jobs/engine.ts`, `worker/jobs/leases.mts`); LlmProvider/ImageProvider wrap existing OpenRouter transports with no depth/segment/inpaint vendors (`.../jobs/providers.ts`, `worker/providers/`); approval of a completed planning checkpoint enqueues exactly the next eligible planning step, visuals only via explicit selection (`.../jobs/pipeline.ts`, `worker/jobs/orchestrate.mts`, `src/app/api/books/[bookId]/jobs/approve/`); idempotent costs keyed `${idempotencyKey}:unit:${unit}` (`.../jobs/costs.ts`); legacy allJobs callers untouched, adapters in `src/features/jobs/adapters.ts` remain until P12.
- Gates: lint pass; build pass (route table includes …/jobs/approve, 25 routes); smoke:job-engine 23/23; smoke:operations 13/13; smoke:batch 4/4; smoke:workflow 10/10. Evidence: "PASS Approve twice yields one successor job" and "PASS Resume runs only the remaining provider call" and "PASS Cost ledger holds one charge per completed unit" — a fixture checkpoint approval advances one planning job, a killed multi-step job resumes without repeating completed calls, and completed units are never double-charged.
- OWNER STEPS REMAIN (qa-pending, do not mark done until): (1) run `npm.cmd run smoke:job-engine` locally and inspect lease-recovery, approval-idempotency and cost-accounting PASS lines; (2) approve a fixture checkpoint twice and confirm exactly one successor planning job; (3) interrupt a fixture multi-step job mid-run, resume it, and confirm retained checkpoints with no duplicate completed-call charges; (4) confirm no `images.*` job exists without an explicit single/batch selection. Unattended run used fakes only; live kill/restart verification belongs to P13. No new dependencies (zod only in scope; three/fiber/playwright untouched). plan.md, prompts.md, CONTEXT.md untouched.
- Patch: `.patches/P4-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P4-new-files/`. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P4: durable job engine, provider boundaries and approval-driven pipeline`.
- Out of scope (logged, not fixed): `smoke:ui`/`smoke:posting` and other non-gate suites not re-run; worker `dispatch.mts` still serves the legacy allJobs map path by design until P12.

### 2026-09-18 — P3 done: canonical source and complete schema foundation (unattended)

- Baseline (pre-edit): P2 done, P1 qa-pending; P3 row in-progress. Tracked diff was runner state + P0/P1/P2 work; untracked was P2 patch artifacts plus `docs/features/25d-schema-foundation/logs/P3-attempt1-181548.log`; `new.md` untouched (still untouched after this phase).
- Dependency check: P2 done permits progress. No placeholder needed; all P3 work landed as real code against fakes/fixtures.
- Read AGENTS.md plus bundled Next guide `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md` before changing Next code (new canonical route uses the documented `await context.params` pattern, matching the approve route).
- New canonical contracts (Zod single source, ADR-004; types inferred): `schemas/source.ts` (SourceRevision, Book, TextChunk, VisualProfile, CHUNK_PARAGRAPH_COUNT=20), `schemas/model.ts` (Entity, Event, VisualReferenceVersion, ReferencePin), `schemas/planning.ts` (StoryMap, Episode, FullMoment, Shot), `schemas/jobs.ts` (Job), `schemas/publishing.ts` (EpisodeManifest, PublishedPointer, BookCatalog, WorkflowAction); full `CompositionSchema` appended to `schemas/composition.ts` (usedIn owns reuse, ADR-011). P2 slice exports untouched.
- New storage layer `src/features/book-experience/storage/`: paths (Firebase Storage canonical.json, ledger, source-versioned chunk keys), hash (sha256 paragraph/canonical), codecs (explicit Firestore Timestamp codecs, dependency-free; unsupported schemaVersions fail), canonical (canonicalizeSource/promote chunking: p000000-based seq scoped to sourceId, ID-range overflow fails, 20-paragraph projections), repository (FakeSourceStore + promoteSource with compare-and-swap expectedActiveSourceId, interruption hook, correction history, downstream invalidation, published snapshots never rewritten), review (reviewSource/approveSource/stageCorrection slice).
- Sources: `permanentParagraphId`/`revisionParagraphIds` now p000000-based (intentional plan-10 change; smoke-workflow-contract updated narrowly with the reason recorded). `edit-ops` gains `paragraph.classify` (kind/isStory); `paragraph.delete` kept staging-only with an ADR-002 note; paragraph-row adds a Classify non-story toggle. `Paragraph` type gains optional kind/isStory (existing staging docs keep parsing).
- Worker: `extraction.mts` gains classifyStagingParagraph/toStagingChapters (EPUB/PDF-text/OCR all land in one classified staging shape; IDs assigned at approval only); `ingest.mts` gains pure previewCanonical seam. ingestSource behavior unchanged.
- Thin routes (`src/app/api/books/`): approve route validates canonicalizeSource before committing (CanonicalError→409), records canonicalHash/canonicalPath on the source plus `canonical: {storagePath, hash}` on the book; no migration or deletion of remote records. New read-only GET canonical review route (hash, counts, chunk list, non-story classification).
- Test seam: `tests/smoke/smoke-canonical.mts` 17/17 (prior art: smoke-pdf-cleaning.mts PASS/FAIL style, smoke-workflow-contract.mjs). Covers fixture approval into immutable canonical text, p000000/chunk-size/pointer inspection, correction marking drafts outdated with published bundles byte-identical, invalid-source + promotion-interruption cases, hash/ID/coverage validation. Package script `smoke:canonical` added. No new dependencies (zod only; three/fiber/playwright untouched).
- Gates: lint pass; build pass (route table 23→24 with …/canonical); smoke:schemas 14/14; smoke:canonical 17/17; smoke:pdf 2/2; smoke:uncertainties 5/5; smoke:workflow 10/10 (extra, justified by the contract-test edit). Evidence: "PASS Correction keeps history and marks dependent drafts outdated" and "PASS Old published bundles remain unchanged after correction" — a fixture source approves into immutable canonical text, every paragraph stays traceable, and a correction revision marks dependent drafts outdated.
- Owner QA (one-line spot check, local only): approve a fixture source with story + non-story paragraphs, inspect p000000/chunk sizes/immutable pointer; approve a correction and confirm old published bundles unchanged — all covered by the green smoke; live approval against real Firebase remains owner-run.
- Patch: `.patches/P3-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P3-new-files/`. No commit (suggested message below), no deploy, no live writes. Note: the runner environment shows P3 new files staged in the index; this phase ran no git add/stage itself and staged nothing deliberately.
- Suggested commit message: `P3: canonical source schemas, storage codecs and approval slice`.
- Out of scope (logged, not fixed): other suites still use p000001-style IDs as opaque fixture data (smoke-e2e live-writing test, smoke-episode-map, smoke-bible, smoke-moment-text, smoke-image-prompt) — they assert no ID-generation behavior and their owning phases P5–P10 migrate them; smoke-e2e/smoke-formats remain forbidden as autonomous gates per Scaffold deferred.

### 2026-09-18 — P2 done: one complete 2.5D scene proof (unattended, attempt 2)

- Baseline (pre-edit, attempt 2): P1 qa-pending; P2 code already landed by attempt 1 and staged (schemas/functions/preview/routes/fixture/tests) with package.json + package-lock.json staged for zod/three/@react-three/fiber/@playwright/test/@types/three. Attempt 1 hit the 2700s watchdog: it re-ran the full Playwright suite repeatedly (3min each) and looped killing the dev server with Unix `ps aux`/`kill %1` job control that cannot work in Windows git-bash while the server kept answering 200 on :8900. Attempt 2 changed no application code; reused the still-running attempt-1 server for endpoint evidence and ran each gate exactly once.
- Diagnosis fix: Playwright is prior art, not a gate — not re-run. Gates are exactly `npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:schemas && npm.cmd run test:preview`, all green this attempt.
- New canonical contracts (Zod single source, ADR-004; types inferred): `src/features/book-experience/schemas/` primitives/paragraph/moment/beat/composition/bundle (+index). Rejects zoom<1, depthMesh without aligned depth, schemaVersion!=1, coverage gaps, quote/dialogue verbatim mismatches.
- New pure functions: `functions/compileBundle.ts` (fixture→EpisodeBundle with ADR-002 coverage check, composition resolution, UTF-16 offset verification), `functions/resolveFrame.ts` (pure camera pose/easing/subtitle/parallax seam), `functions/subtitles.ts` (escaped HTML subtitles, ADR-009; only its output reaches dangerouslySetInnerHTML).
- Local preview, no cloud writes: `src/app/api/studio/preview/route.ts` reads only fixtures/book-experience/*.json (404 unknown fixture, 500 unactionable render); `src/app/studio/preview/page.tsx` thin Suspense wrapper. Fixture `room-entry.json` (4 paragraphs: 3 story p000000–p000002 + 1 classified non-story, one Moment ep_01_m01, 3 Beats, composition cmp_room_entry_v1 with L0/L1 planes + L2 depthMesh foreground) labelled "deterministic local fake; no paid generation; no model-quality claim".
- Player `preview/PreviewPlayer.tsx` (dynamic DepthScene, SSR-off) exposes only scope-episode/moment/beat, play, pause, scrub, next, previous, beat-select, fallback-toggle; no camera authoring inputs; reduced-motion static frames. `DepthScene.tsx` renders one runtime depthMesh (three.js) + layer-list; `CssFallback.tsx` flat parallax css-stage with per-layer css-layer-* ids.
- Gates: lint pass; build pass (route table includes /studio/preview + /api/studio/preview); smoke:schemas 14/14 ("EpisodeBundle parse/compile round-trip is stable", "resolveFrame is deterministic"); test:preview 23/23 (endpoint fixture-scoped + no cloud writes, all 11 controls present, no camera authoring, files <1000 lines). Evidence this attempt: endpoint returns provenance "fixture room-entry: deterministic local fake…" with ep_01 / 3 Beats ep_01_m01_b01–b03 and exact quote "Mara pushed the door open…"; page shell serves preview-player.
- Browser evidence (attempt 1, identical tree — no application edit since): `npx playwright test tests/book-experience/preview.spec.ts` 8/8 green covering provenance label, HTML subtitles, play-every-Beat with WebGL-or-CSS stage, scrub, Moment/Episode scope switch, forced CSS fallback with same text, portrait+landscape readability, no camera input, reduced-motion note. Not re-run in attempt 2 (watchdog cause); spec + config unchanged and committed in this phase.
- Demoable outcome met: "An editor opens one bundled scene, reads its HTML subtitles and previews the same Beats with WebGL depth meshes or CSS layers." Owner QA remains a one-line spot check: open /studio/preview?fixture=room-entry, play 3 Beats, toggle fallback, resize — all covered by the green spec.
- No new dependencies beyond the approved five; plan.md, prompts.md, CONTEXT.md untouched; no secrets in diff; `new.md` untouched. Out of scope (logged, not fixed): untracked `test-results/` playwright output left from attempt 1; stale dev server on :8900 from attempt 1 left running (reused read-only, never killed).
- Patch: `.patches/P2-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P2-new-files/`. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P2: deterministic 2.5D scene proof with schemas, local preview and browser prior art`.

### 2026-09-18 — P1 qa-pending: development access without sign-in (unattended)

- Baseline (pre-edit): P0 done; tracked diff was only runner state + P0 row work, untracked only runner logs and P0 patch artifacts; `new.md` untouched. P0 staged entries left intact; P1 work is unstaged only.
- Read AGENTS.md plus bundled Next guides `node_modules/next/dist/docs/01-app/02-guides/authentication.md` and `01-app/03-api-reference/03-file-conventions/proxy.md` before changing Next code (proxy.ts keeps the documented `proxy` export + matcher; Proxy stays an optimistic check, layout verifies).
- New pure test seam (no Next/Firebase deps): `src/features/auth/mode.ts` (resolveAuthMode/production-forced, DEV_SESSION_UID/DEV_IDENTITY_UID) and `src/features/auth/policy.ts` (proxyDecision, acceptSession with dev rejection at production boundaries, canMintDevelopmentIdentity, isDevelopmentMarker).
- `constants.ts` now delegates to the trusted mode decision; production always returns authenticated even with STUDIO_AUTH_DISABLED=1. `session.ts` keeps the dev fallback only in bypass and rejects uid `dev` on the authenticated path. `proxy.ts` uses the same decision. Session mint route rejects literal development markers (403).
- New trusted endpoints: `GET /api/auth/mode` (reveals mode only) and `POST /api/auth/dev-token` (404 outside bypass; mints a Firebase custom token with the allowlisted editor email for the browser; never sets a session cookie; no shipped credentials; rules untouched).
- Browser: `dev-identity.ts` auto sign-in via signInWithCustomToken with actionable errors; `use-live.ts` adds useAuthMode/useStudioReadiness/describeDataError. Dashboard drops its duplicate onAuthStateChanged, gates subscriptions on readiness and shows one actionable role=alert error. book-detail/extraction-editor switch to useStudioReadiness (one line each). Layout passes bypassed to StudioShell/Sidebar; sign-out hidden when bypassed.
- Gates: lint pass; build pass (route table 18→20 with /api/auth/mode + /api/auth/dev-token); smoke:auth 32/32 (new, fakes only — never calls dev-token); smoke:workflow 9/9; smoke:ui 6/6. Evidence: "PASS Production proxy rejects anonymous access without a session cookie" and "PASS Development proxy lets a fresh browser through with no cookie" (smoke:auth, local policy proof — no browser was opened unattended).
- No new dependencies (zod/three/fiber/playwright untouched — not needed for auth/readiness). No secrets in diff; firestore.rules unchanged; plan.md, prompts.md, CONTEXT.md untouched.
- OWNER STEPS REMAIN (qa-pending, do not mark done until): (1) owner configures local dev Firebase identity/credentials (STUDIO_ALLOWED_EMAILS + Firebase project, server-side only); (2) open a fresh local browser at next dev and confirm Studio loads with live-data readiness and no login; (3) set STUDIO_AUTH_DISABLED=1 with NODE_ENV=production and confirm anonymous /studio access is still rejected and no dev token is minted (expect 404); (4) confirm authenticated production isolation with the allowlisted editor sign-in. Unattended run exercised no live identity issuance and opened no browser.
- Patch: `.patches/P1-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P1-new-files/`. Changed tracked files: package.json, proxy.ts, auth constants/session, session route, studio layout, sidebar, studio-shell, dashboard, book-detail, extraction-editor, use-live.ts. New files: auth mode.ts/policy.ts, api/auth/mode + dev-token routes, services/firebase/dev-identity.ts, tests/smoke/smoke-auth.mts. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P1: development access without sign-in via trusted server auth mode`.
- Out of scope (logged, not fixed): `useFirebaseUser` remains exported for compat but no component consumes it directly anymore; dev-token endpoint behavior against live Firebase is owner QA only.

### 2026-09-18 — P0 done: behavior-neutral preparation

- Baseline (pre-edit, all green): `npm.cmd run lint` pass; `npm.cmd run build` pass with identical route table; `smoke:workflow` 9/9; `smoke:ui` 6/6; `smoke:operations` 12/12. Tracked baseline diff was only the runner's P0 in-progress row; untracked baseline was only runner logs (`.run/`, `.runinfo`, `logs/`, `loop-run-20260918-171500.log`); `new.md` preserved untouched.
- Read AGENTS.md plus the bundled Next guide `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md` before changing Next code; split keeps every file a `"use client"` component with identical JSX/routes/approval actions.
- Split `src/features/images/components/image-studio.tsx` (916 lines) into a 225-line orchestrator plus `studio-stage.tsx` (Stage/ReferenceGroup), `studio-canon-editor.tsx` (FIELD_LABEL/EMPTY_CANON/CanonEditor), `studio-reference.tsx` (REFERENCE_STAGES/ReferenceCreator/ReferenceCard), `studio-episode-images.tsx` (EpisodeImages/EpisodeMomentImages/MomentImageCard). All 11 visual action types (`canon.propose/save/approve/reopen`, `reference.create/generate/approve`, `image.generate/select/metadata/batch.generate`) preserved verbatim; sole consumer `extraction-editor.tsx` import unchanged.
- Extracted `worker/index.mts` (635 lines) into a 202-line poll/claim/process/presence entry plus `worker/jobs/job-store.mts` (ClaimedJob/StoredJob/storedJobs/patchJob), `queue.mts` (claimNextJob/requeueStaleJobs/enqueueFollowUp/progressReporter), `dispatch.mts` (12-type runJob switch incl. test.echo), `recovery.mts` (sourceRef/markWorkFailed/failureSuggestion). allJobs map shape, budget waiting_confirmation gate, idempotency/resume-from-checkpoint, costs and per-type failure marking unchanged; every new file <300 lines.
- Adapted two source-string tests to follow the extracted modules without weakening claims: `smoke-operations.mts` and `smoke-workflow-contract.mjs` now assert against combined `worker/index.mts` + `worker/jobs/*.mts` sources (same regexes), plus narrow checks that the entry delegates to `./jobs/queue|dispatch|recovery` and passes `jobsCollection: JOBS` into `collectionGroup`.
- Post-edit gates: lint pass; build pass (same 18 routes); smoke:workflow 9/9; smoke:ui 6/6; smoke:operations 13/13; `node --check` clean on all five worker files. Evidence: "The existing editorial workflow remains usable with no behavior change and its baseline checks still pass" — 28/28 smoke checks green after a verbatim move with zero route/type/action changes.
- QA mapping (unattended, no browser opened): smoke:ui still asserts tab order, JobProgress aria-live/progressbar and image-workspace selectors; owner spot-check of Studio tabs/image selection/job progress recommended at next dev session but no code awaits it.
- Patch: `.patches/P0-studio.patch` (tracked binary diff) plus new-file copies under `.patches/P0-new-files/`. Changed tracked files: image-studio.tsx, worker/index.mts, smoke-operations.mts, smoke-workflow-contract.mjs, status.md. New files: 4 studio components + 4 worker/jobs helpers. No staging, no commit (suggested message below), no deploy, no live writes.
- Suggested commit message: `P0: behavior-neutral split of image studio and worker queue helpers`.
- No new dependencies added (zod/three/fiber/playwright not needed for a behavior-neutral split); plan.md, prompts.md, CONTEXT.md untouched; no secrets in diff; no scope creep.

### 2026-09-18 — Scaffold

- Owner confirmed the plan after adding AI-only authoring, approval-driven preview, complete-book representation, HTML subtitles, immutable reference assets, independent episode publication/replacement, AWS visual storage and development auth bypass.
- Created the plan, self-contained phase prompts, vocabulary/ADRs, status tracker and rendered unattended runner.
- No implementation phase started; no application/auth changes, model calls, live writes, commits or deployment.
- Verification passed: Bash syntax; 14 matching phase rows, dependencies, sequential waves, self-contained prompt blocks, QA/gate declarations and HUMAN-ONLY detection; all five files below 1000 lines with LF encoding; canonical TypeScript contract examples type-check together in memory.
- Read-only runner preflight completed and recognized all 14 phases and 13 automatic gate commands. It did not dispatch agents or create run state.
- The sandboxed preflight could not access the Codex home and incorrectly reported unauthenticated. A separate authorized read-only host check returned `Logged in using ChatGPT`; this is an environment restriction, not missing CLI authentication.
- No application lint/build/smoke gate was run for this documentation-only change. Runtime implementation, image quality and real-provider behavior remain unverified until their phases execute.

## Deferred

- [Scaffold] D-1: owner must prove the selected image model produces aligned, consistent alpha/depth assets; fixture tests cannot resolve this. Blocks real-asset acceptance in P13 only.
- [Scaffold] P11: current rules tests are source-text checks. Local emulator behavioral proof may require owner environment preparation; deployed rule verification belongs to P13.
- [Scaffold] Current tests/smoke/smoke-e2e.mts and smoke-formats.mts authenticate and write real cloud records. Do not use them as autonomous gates against .env.
- [Scaffold] WindowsApps python3 alias failed; real interpreter verified at C:/Users/800sa/AppData/Local/Python/bin/python3.exe. Runner Windows configuration prioritizes it.
- [loop] P0 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:workflow && npm.cmd run smoke:ui && npm.cmd run smoke:operations
- [P1 unattended 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Live dev-identity issuance (POST /api/auth/dev-token against real Firebase) and fresh-browser verification deliberately not exercised; both are explicit owner QA in the Progress log. No new architectural decision proposed.
- [loop] P1 qa-pending — complete, awaiting the owner's eyes: Code complete unattended: unified server auth mode, dev-only auto browser identity, bypass-aware readiness, sign-out hidden when bypassed, production forced even with flag. Owner must verify fresh-browser dev access and production rejection (steps in Progress log); live identity issuance never exercised. Patch .patches/P1-studio.patch + new-file copies.
- [loop] P2 watchdog timeout after 2700s (attempt 1)
- [loop] P2 done 2026-09-18 (attempt 2, no application-code change): root cause of the attempt-1 timeout was repeated full Playwright runs plus Unix-style dev-server kill loops on Windows git-bash. Fix applied: Playwright treated as prior art (8/8 evidence from attempt 1 on the identical tree stands), each of the four phase gates run exactly once, leftover :8900 server reused read-only. No plan contradiction; plan.md/CONTEXT.md untouched.
- [loop] P2 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:schemas && npm.cmd run test:preview
- [P3 unattended 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Intentional plan-10 change (p000001→p000000 IDs, deletion→classification) applied with narrow test updates and recorded reasons. Live approval/canonical.json materialization against real Firebase deliberately not exercised; fakes/local repositories only. No new architectural decision proposed.
- [loop] P3 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:schemas && npm.cmd run smoke:canonical && npm.cmd run smoke:pdf && npm.cmd run smoke:uncertainties
- [loop] P3 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:schemas && npm.cmd run smoke:canonical && npm.cmd run smoke:pdf && npm.cmd run smoke:uncertainties
- [loop] provider-capacity brake — fleet held 30m (all loops resume automatically)
- [loop] pause expired — resuming
- [loop] provider-capacity brake — fleet held 30m (all loops resume automatically)
- [loop] pause expired — resuming
- [loop] provider-capacity brake — fleet held 30m (all loops resume automatically)
- [loop-pilot 2026-09-18] Preflight: owner confirmed P1 browser checks — P1 qa-pending → done. P4/P5/P9/P11 cleared for unattended attempt; P13 stays HUMAN-ONLY. Resuming with AGENT=openrouter, MAX_PARALLEL=1.
- [P4 unattended 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Gate repairs only: strip-types-incompatible constructors expanded, smoke-test store isolation, added smoke:job-engine script, smoke:workflow script flag alignment (content unchanged). Live queue/paid providers/budget authority untouched; live kill/restart verification belongs to P13. No new architectural decision proposed.
- [loop] P4 qa-pending — complete, awaiting the owner's eyes: Code complete unattended: durable job documents with leases/checkpoints/retries, provider boundaries, approval-driven planning succession, idempotent costs. Owner must verify lease recovery, approval idempotency and cost evidence in fixture runs and confirm no visual auto-start (steps in Progress log). Patch .patches/P4-studio.patch + new-file copies.
- [loop] provider-capacity brake — fleet held 30m (all loops resume automatically)
- [P4 verification 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Zero source edits this attempt: P4 implementation (commits 972b1ff, 97bc450) re-read and re-gated green, P4 row moved in-progress → qa-pending with verified route count (24 app routes + proxy). No new architectural decision proposed.
- [loop] provider-capacity brake — fleet held 30m (all loops resume automatically)
- [loop] pause expired — resuming
- [P4 verification-3 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Zero source edits: P4 implementation (commits 972b1ff, 97bc450) re-read and re-gated green (lint; build 24 routes + proxy; job-engine 23/23; operations 13/13; batch 4/4; workflow 10/10), P4 row moved in-progress → qa-pending. No new architectural decision proposed.
- [loop] P4 qa-pending — complete, awaiting the owner's eyes: Code complete unattended (commits 972b1ff, 97bc450; zero source edits this attempt): one-document-per-job engine, provider boundaries, approval pipeline, lease recovery, idempotent costs; owner must verify lease recovery, approval idempotency and cost evidence in fixture runs plus confirm no visual auto-start (steps in Progress log); live kill/restart belongs to P13. Patch .patches/P4-studio.patch + new-file copies.
- [P5 unattended 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Inline alias/flashback fixtures only (no paid understanding calls); no-fabricated-facts rule enforced by quote-grounding rejection test. Live approval against real Firestore deliberately not exercised; fakes/local repositories only. Real-book model quality is P13 owner QA. No new architectural decision proposed.
- [loop] P5 qa-pending — complete, awaiting the owner's eyes: Code complete unattended: chapter-bounded rolling ledger with overlap dedup, deterministic deltas, immutable per-window checkpoints, consolidated Entity/Event review with spoiler-safe identity, AI merge/split proposals with approve/reject-only actions. Owner must inspect deterministic replay, evidence validity and retained approvals in fixture runs (steps in Progress log); real-book model quality belongs to P13. Patch .patches/P5-studio.patch + new-file copies.
- [loop] P5 qa-pending — complete, awaiting the owner's eyes: Code complete unattended: rolling ledger + Book Model review; owner must inspect deterministic replay, evidence validity and retained approvals in fixture runs plus approve/reject/regenerate without entering data (steps in Progress log); real-book model quality belongs to P13. Patch .patches/P5-studio.patch + new-file copies.
- [P6 unattended 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Intentional plan-10 change (legacy p000001/intended-overlap/many-to-many → canonical p000000 strict tiling) applied via adapters with retained internals and recorded reasons. Live approval against real Firestore deliberately not exercised; fakes/local repositories only. Real-book planning quality is P13 owner QA. No new architectural decision proposed.
- [loop] P6 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:story-planning && npm.cmd run smoke:episodes && npm.cmd run smoke:moments
- [P7 unattended 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. One test-authoring correction (seq-gating check gated out every entity instead of expecting an allowed entity to vanish); no behavior change. Live approval against real Firestore deliberately not exercised; fakes/local repositories only. Real-book beat quality is P13 owner QA. No new architectural decision proposed.
- [loop] P7 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:beats && npm.cmd run smoke:text && npm.cmd run smoke:posting && npm.cmd run test:preview
- [P8 unattended 2026-09-18] No plan contradiction found; plan.md/CONTEXT.md untouched. Gate repairs only: removed a non-existent `lens` field from the StyleCanon smoke fixture (build type-check) and expanded a TS parameter property the strip-types runner cannot parse (same class as the P4 fix). Live image generation, S3 writes and paid model calls deliberately not exercised; fakes/local fixtures only. Real-model alpha/depth/continuity quality is P13/D-1 owner QA. No new architectural decision proposed.
- [loop] P8 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:visual-generation && npm.cmd run smoke:images && npm.cmd run smoke:batch && npm.cmd run smoke:visual
- [loop] P8 done — gate passed: npm.cmd run lint && npm.cmd run build && npm.cmd run smoke:visual-generation && npm.cmd run smoke:images && npm.cmd run smoke:batch && npm.cmd run smoke:visual
- [loop] P9 qa-pending — complete, awaiting the owner's eyes: Code complete unattended: assembly, safeCamera, Episode preview with blockers and approve-and-continue; owner must play all scopes, test extremes/crops/motion/fallback, follow a rejected layer to regeneration, and visually certify art quality (steps in Progress log); paid regeneration never exercised. Patch .patches/P9-studio.patch + new-file copies.
- [loop] reset stale in-progress P10 to blocked (left by a previous interrupted run — inspect its diff/patches before rerunning)
- [loop] pause expired — resuming
- [loop] forward sweep engaged — phases stranded behind failed or human-only work will run once with a degraded-dependency notice
- [loop] pause expired — resuming
- [loop] forward sweep: dispatching P11 over degraded dependencies (P10(blocked: Not started.))
- [loop] forward sweep engaged — phases stranded behind failed or human-only work will run once with a degraded-dependency notice
- [loop] forward sweep: dispatching P11 over degraded dependencies (P10(blocked: Not started.))
- [loop] reset stale in-progress P11 to blocked (left by a previous interrupted run — inspect its diff/patches before rerunning)
- [loop] forward sweep engaged — phases stranded behind failed or human-only work will run once with a degraded-dependency notice
- [loop] forward sweep: dispatching P12 over degraded dependencies (P11(blocked: Not started.))
- [P12 unattended 2026-09-19] No plan contradiction found; plan.md, prompts.md, CONTEXT.md and new.md untouched. Six dead temporary adapters were contracted only after their test callers moved to canonical contracts. No cloud data, live asset, rule, deployment or owner file was changed.
- [P12 deferred 2026-09-19] P10 is absent: implement canonical publishing/bundle/catalog/pointer/replacement/rollback contracts and add `smoke:bundles` plus `smoke:publishing`; until then the full fixture cannot prove independent publication or that published version 1 survives draft regeneration.
- [P12 deferred 2026-09-19] P11 is absent: implement and behaviorally verify canonical editor/private versus sanitized published-pointer access with a local emulator; static `smoke:posting` rules checks are supplementary only.
- [P12 deferred 2026-09-19] Active legacy consumers remain required: worker/costs/tests still use `jobs/allJobs`, and the primary source workflow still imports BibleEntry/EpisodePlan/StoryMomentPlan stores/components with required editorial forms. Migrate those callers in their owning phases before deleting the paths; P12 deliberately did not break supported extraction/model-settings/cost behavior or mislabel them retired.
- [loop] P12 qa-pending — independent code landed against an explicit P10/P11 placeholder: canonical P3–P9 workflow fixture 10/10, six uncalled adapters removed, 26/28 listed commands pass; `smoke:bundles` and `smoke:publishing` remain missing with P10. Patch `.patches/P12-studio.patch` + new-file copy.
- [loop] P12 qa-pending — complete, awaiting the owner's eyes: P3–P9 canonical fixture 10/10 through approvals, single+batch visuals, composition and all preview scopes; six uncalled temporary adapters contracted. Gaps: P10 publish/replace + bundle scripts absent; P11 rules absent; active primary flow still requires legacy allJobs/BibleEntry/EpisodePlan/StoryMomentPlan paths, so no claim that published v1 survives regeneration or that every retired path is gone. Patch `.patches/P12-studio.patch` + `.patches/P12-new-files/`.
- [loop] run INCOMPLETE — 16 QA item(s) for the owner, 3 defect(s)
